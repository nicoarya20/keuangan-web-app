# Task: Prisma Setup and Schema Modeling

This task list tracks the progress of installing Prisma and defining the database schema.

## Tasks
- [x] **1. Install Dependencies**
    - [x] Run `npm install prisma --save-dev`
    - [x] Run `npm install @prisma/client`
- [x] **2. Initialize Prisma**
    - [x] Run `npx prisma init --datasource-provider sqlite`
- [x] **3. Define Schema Models**
    - [x] Define `Income` model in `schema.prisma`
    - [x] Define `Expense` model in `schema.prisma`
    - [x] Define `WishlistItem` model in `schema.prisma`
    - [x] Define `Saving` model in `schema.prisma`
    - [x] Define `CategoryBudget` model in `schema.prisma`
- [x] **4. Generate & Migrate**
    - [x] Run `npx prisma generate`
    - [x] Run `npx prisma migrate dev --name init`

## Status
- Overall Status: Selesai
- Started: March 29, 2026
- Finished: March 29, 2026
